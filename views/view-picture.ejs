<div class="container mt-3">
    <h4>الوصف</h4>
    <p class="mt-1"><%= picture.prompt %></p>
    <h4 class="mt-4">الصور</h4>
    <div class="row mt-1">
            <% picture.pictures.forEach((pic) => {%>
                <div class="col-4">
                    <div class="card text-bg-dark">
                    <img src="<%=pic.url%>" class="card-img" alt="...">
                    </div>
                </div>
            <%})%>
    </div>
    <h4 class="mt-4">الإجابات</h4>
    <ul class="list-group mt-1">
        <%picture.answers.forEach(answer=>{%>
            <li class="list-group-item">
                <%=answer.correct ? '✓' : ''%>&nbsp;&nbsp;<%=answer.text%>
            </li>
        <%})%>
    </ul>
</div>
